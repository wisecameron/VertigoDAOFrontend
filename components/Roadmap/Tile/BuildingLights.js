export default function BuildingLights(props)
{
    for(let i = 0; i < props.stripLength; i++) //for each row of buildings
    {
        for(let j = 0; j < 2; j++) //for each building 
        {
            //Add random light positions around visible portion of building.
            
        }
    }
}